{{#ui-block/rows as |rows|}}

  {{#if query.isLoading}}

    {{#rows.header}}
      Loading…
    {{/rows.header}}

  {{else}}

    {{#rows.header}}
      {{query}}
    {{/rows.header}}

    {{#each query.content as |person|}}
      {{#rows.row}}
        {{person.doc.path}} {{person.name}}
      {{/rows.row}}
    {{/each}}

  {{/if}}

{{/ui-block/rows}}
